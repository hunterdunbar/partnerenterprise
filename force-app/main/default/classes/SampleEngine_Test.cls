@isTest
public with sharing class SampleEngine_Test {
    static testMethod void testSampleEngine(){
        SampleEngine sampleClass = new SampleEngine();
        System.Assert.areEqual(true, true);
    }
}